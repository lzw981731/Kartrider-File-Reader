name: Build RhoLoader

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore src/RhoLoader/RhoLoader.csproj

      - name: Build RhoLoader (Release x86)
        run: dotnet build src/RhoLoader/RhoLoader.csproj -c Release -p:Platform=x86 --no-restore

      - name: Build KartLibrary (Release AnyCPU)
        run: dotnet build src/KartriderLibrary/KartLibrary.csproj -c Release --no-restore

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: RhoLoader-build
          path: |
            src/RhoLoader/bin/Release/net8.0-windows7.0/win-x86/
            src/RhoLoader/bin/Release/
          retention-days: 14